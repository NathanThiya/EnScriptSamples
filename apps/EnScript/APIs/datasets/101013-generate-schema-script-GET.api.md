# Generate Schema Script for all Datasets in the workspace.

This script will not have the data.
This script can be executed to create all datasets.
The datasets that exists will not be created.

```
<% en.data.sets.generateSchemaScript results %>
<% return 'generateSchemaScript' results %>
```
